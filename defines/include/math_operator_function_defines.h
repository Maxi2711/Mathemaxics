#pragma once

#include "math_type_defines.h"

tNumber add_num(tNumber a, tNumber b);
tNumber sub_num(tNumber a, tNumber b);

tNumber mult_num(tNumber a, tNumber b);
tNumber div_num(tNumber a, tNumber b);
