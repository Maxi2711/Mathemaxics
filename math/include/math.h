#pragma once
#include "../../defines/include/defines.h"

tNumber add(tNumber a, tNumber b);
tNumber subtract(tNumber a, tNumber b);
tNumber multiply(tNumber a, tNumber b);
tNumber divide(tNumber a, tNumber b);
